<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions for camera and storage access -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Delivery_app"
        tools:targetApi="31">

        <!-- Activities Declaration -->
        <activity android:name=".Activity.Dashboard" android:exported="false" />
        <activity android:name=".Activity.CreateEntretien" android:exported="false"/>
        <activity
            android:name=".Activity.CreateCandidatureActivity"
            android:exported="true"/>

        <activity android:name=".Activity.AddOffreActivity" android:exported="true"/>
        <activity android:name=".Activity.ListOffreActivity" android:exported="false"/>
        <activity android:name=".Activity.ListEntretiensActivity" android:exported="false"/>
        <activity android:name=".Activity.EntretienAdapter" android:exported="false"/>
        <activity android:name=".Activity.ModifyEntretienActivity" android:exported="false"/>
        <activity android:name=".Activity.Profile" android:exported="false" />
        <activity android:name=".Activity.Forget_pwd" android:exported="false" />
        <activity android:name=".Activity.Login" android:exported="false" />
        <activity android:name=".Activity.Create_Account_User" android:exported="false" />
        <activity android:name=".Activity.Page3" android:exported="false" />
        <activity android:name=".Activity.Page2" android:exported="false" />

        <!-- Launcher Activity with MAIN and LAUNCHER intent filter -->
        <activity android:name=".Activity.page1" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
